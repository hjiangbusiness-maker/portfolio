# portfolio.rndv.world

Deploy target for the RNDV portfolio page (GitHub Pages). PUBLIC repo: the only content that ships is a password-gated ciphertext page. Source of truth lives in the vault (`artifacts/portfolio-simulator.html`); regenerate with `tools/publish-page.py`.
